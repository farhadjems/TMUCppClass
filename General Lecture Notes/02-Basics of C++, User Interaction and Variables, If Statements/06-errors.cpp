#include <iostream>

using namespace std;

int main()
{
    a = 10; // a is not defined !
    int b // semicolon ; is forgotten!

    int x;
    int y;
    y = 5;
    x += y; // x is not initialized, anything might be printed on terminal!
    cout << "x is: " << x << endl;

    int c; 
    int C; // C++ is case sensitive, so that C is different with c!


    //*naming variables:
    //* Choosing meaningful, descriptive names for your variables is also very important
    //* it will help understand the code!
    int val1, val2, val3;
    val1 = val2 * val3; // what are these!

    int area, height, width;
    area = height * width; // aha, this is the area formula :D

}