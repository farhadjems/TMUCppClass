#include <iostream> 

using namespace std;

int addition(int x, int y)
{
    return (x + y);
}

int subtraction(int x, int y)
{
    return (x - y);
}

int multiplication(int x, int y)
{
    return (x * y);
}

double division(int x, int y)
{
    return (x / y);
}

int main()
{
    //* as you may considered, defining all functions in one file
    //* can be confusing, its getting harder to understand and harder to debug

    //* we can move them into another file and use them here! 
}