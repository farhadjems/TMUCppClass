//* to separate files, you need a file to define functions and variables
//* this file must include header file which these functions and variabls 
//* are delcared in them

#include "10-arithmeticHeader.h"

int addition(int x, int y)
{
    return (x + y);
}

int subtraction(int x, int y)
{
    return (x - y);
}

int multiplication(int x, int y)
{
    return (x * y);
}

double division(int x, int y)
{
    return (x / y);
}